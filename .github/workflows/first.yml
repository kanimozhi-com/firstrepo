name: Hello Workflow

on:
  workflow_dispatch:  # allows manual trigger

jobs:
  say-hello:
    runs-on: self-hosted  # run on your own machine
    
    steps:
      - name: Print Hello
        run: echo "👋 Hello from your self-hosted runner!"

